/*---------------------------------------------------------
 * Copyright (C) Microsoft Corporation. All rights reserved.
 *--------------------------------------------------------*/
"use strict";
var stringjs = require("string");
var stringPrefix = "s:";
var doublePrefix = "d:";
function interpretSerializedValue(input) {
    if (stringjs(input).startsWith(doublePrefix)) {
        var doubleString = input.substring(doublePrefix.length);
        var parsedNumber = Number(doubleString);
        if (!isNaN(parsedNumber)) {
            return parsedNumber;
        }
    }
    else if (stringjs(input).startsWith(stringPrefix)) {
        return input.substring(stringPrefix.length);
    }
    return input;
}
exports.interpretSerializedValue = interpretSerializedValue;
function prepareValue(input) {
    if (typeof input === "number") {
        return "" + doublePrefix + input;
    }
    else if (typeof input === "string") {
        return "" + stringPrefix + input;
    }
}
exports.prepareValue = prepareValue;
